# SQLite used for local development; set DATABASE_URL for PostgreSQL in production
DATABASE_URL=postgres://user:password@localhost:5432/gestor_taller
JWT_SECRET=change_this_secret
PORT=3000
NODE_ENV=production

# For Railway: set the `DATABASE_URL` variable provided by Railway for your Postgres plugin.
# Ensure `JWT_SECRET` is set to a strong random value in production.
